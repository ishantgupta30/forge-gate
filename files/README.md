See the top-level [README.md](../README.md) for setup, architecture, and API docs.

This `files/README.md` previously described an earlier planning draft (GMI Cloud,
`backend/` paths) that no longer matches the shipped code — removed to avoid two
READMEs disagreeing with each other.
