# Deprecated: use the root-level requirements.txt instead.
# This file previously listed torch/open_clip/genblaze-gmicloud/genblaze-replicate,
# none of which the current code (imagehash-based duplicate check, Pollinations +
# Hugging Face providers) actually uses. Removed to avoid a misleading/broken
# `pip install` for anyone reading files/ directly.
-r ../requirements.txt
